 
  public class httpManage extends AppCompatActivity {
    Button buttons;
    EditText putText;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_http_manage);
    }

    public void doOn(View view)
    {
 //Asyc class name      //this is used for reference
       getf getf = new getf(this);
        getf.execute();
        
        }
        }
